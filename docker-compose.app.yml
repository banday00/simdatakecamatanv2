services:
  app:
    container_name: simdata-app
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL}
        - NEXT_PUBLIC_DEFAULT_TENANT_SLUG=${NEXT_PUBLIC_DEFAULT_TENANT_SLUG}
        - NEXT_PUBLIC_UPLOAD_BASE_URL=${NEXT_PUBLIC_UPLOAD_BASE_URL:-/uploads}
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://${POSTGRES_USER:-simdata}:${POSTGRES_PASSWORD:-simdata}@simdata-db:5432/${POSTGRES_DB:-simdata}
      - AUTH_SECRET=${AUTH_SECRET}
      - AUTH_URL=${AUTH_URL}
      - NEXT_PUBLIC_APP_URL=${NEXT_PUBLIC_APP_URL}
      - NEXT_PUBLIC_DEFAULT_TENANT_SLUG=${NEXT_PUBLIC_DEFAULT_TENANT_SLUG}
      - NEXT_PUBLIC_UPLOAD_BASE_URL=${NEXT_PUBLIC_UPLOAD_BASE_URL:-/uploads}
      - UPLOAD_DIR=${UPLOAD_DIR:-/app/uploads}
      - N8N_WEBHOOK_PAJAK_KENDARAAN=${N8N_WEBHOOK_PAJAK_KENDARAAN}
      - N8N_WEBHOOK_PBB=${N8N_WEBHOOK_PBB}
    volumes:
      - simdata-uploads:/app/uploads
    networks:
      - simdata-network
    env_file:
      - .env.production

volumes:
  simdata-uploads:
    driver: local

networks:
  simdata-network:
    external: true
